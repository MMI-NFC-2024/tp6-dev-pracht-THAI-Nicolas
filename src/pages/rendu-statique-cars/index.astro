---
import Layout from "../../layouts/Layout.astro";
import cars from "../../assets/cars.json";

const carBrands = [...new Set(cars.map(car => car.name.split(' ')[0]))].sort();
---

<Layout>
  <div class="min-h-screen bg-gradient-to-br from-slate-900 via-orange-900 to-red-900 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-r from-orange-600/10 via-red-500/5 to-pink-600/10"></div>
    <div class="absolute top-0 right-0 w-96 h-96 bg-gradient-to-br from-orange-400/30 to-red-600/30 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-80 h-80 bg-gradient-to-tr from-red-500/20 to-pink-600/20 rounded-full blur-3xl"></div>
    
    <div class="relative z-10 container mx-auto px-6 py-12">
      <div class="text-center mb-12">
        <h1 class="text-5xl font-bold mb-4 bg-gradient-to-r from-orange-400 to-red-400 bg-clip-text text-transparent">
          🚗 Marques de Voitures
        </h1>
        <p class="text-xl text-orange-200/80 font-light">
          Rendu Statique - Cliquez sur une marque pour voir les détails
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        {carBrands.map((brand) => {
          const brandCars = cars.filter(car => car.name.startsWith(brand));
          return (
            <a 
              href={`/rendu-statique-cars/${brand.toLowerCase()}`}
              class="group bg-black/20 backdrop-blur-md rounded-2xl border border-white/25 p-6 hover:bg-black/30 transition-all duration-300 shadow-lg hover:scale-105 hover:shadow-orange-500/30"
            >
              <div class="text-center">
                <div class="text-6xl mb-4 group-hover:scale-110 transition-transform duration-300">
                  🚗
                </div>
                <h2 class="text-2xl font-bold text-white mb-2 drop-shadow-lg">
                  {brand}
                </h2>
                <p class="text-orange-200/80 text-sm">
                  {brandCars.length} modèle{brandCars.length > 1 ? 's' : ''}
                </p>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </div>
</Layout>
